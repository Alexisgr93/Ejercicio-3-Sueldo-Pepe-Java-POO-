//toda clase que implemente IBONO, va a tener un metodo que calcule MontoBono
// y ese monto va a recibir la cantidad de faltas
public interface IBono {
	
	//metodo de IBono
	
	public double MontoBono(int faltas);

}
